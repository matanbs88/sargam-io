import { StudioDirection } from '@/src/features/design-lab/StudioDirection';

export const metadata = { title: 'Studio direction · Sargam design lab', robots: { index: false, follow: false } };

export default function Page() {
  return <StudioDirection />;
}
